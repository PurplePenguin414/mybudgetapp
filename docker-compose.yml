version: '3.8'

services:
  budget-dashboard:
    build: .
    container_name: budget-dashboard
    restart: unless-stopped
    ports:
      - "127.0.0.1:3011:3000"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
